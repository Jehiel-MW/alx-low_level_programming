#include "main.h"

/**
* _abs -> computing the absolute value of given set of data
* @int: compute absolute value of an integer
* Return: 0 (success)
*/

int _abs(int)
{
if (n < 0)
n = -n;
return (n);
}
