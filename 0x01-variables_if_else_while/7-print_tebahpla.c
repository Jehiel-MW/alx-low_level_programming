#include <stdio.h>

/**
* main -> assign a random number to the variable n each time it is executed
* Return: always 0
*/
int main(void)
{
int ch;
for (ch = 122 ; ch >= 97 ; ch--)
{
putchar(ch);
}
putchar(10);
return (0);
}
