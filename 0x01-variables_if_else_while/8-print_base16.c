#include <stdio.h>

/**
* main -> assign a random number to the variable n each time it is execute
* Return: always 0
*/
int main(void)
{
int i;
for (i = 1 ; i <= 16 ; i++)
{
putchar(i);
}
putchar(10);
return (0);
}
